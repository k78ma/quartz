---
title: Sources of Test Error
tags:
  - dl
date: 2025-08-18
aliases:
  - sources of test error
  - noise
  - bias
  - variance
---
With sufficient capacity, a neural network often performs perfectly on training data; however, it does not necessarily generalize well to new test data.

We will see that the test errors have three distinct causes and that their relative contributions depend on (i) the inherent uncertainty in the task, (ii) the amount of training data, and (iii) the choice of model. The latter dependency raises the issue of hyperparameter search. We discuss how to select both the model hyperparameters (e.g., the number of hidden layers and the number of hidden units in each) and the learning algorithm hyperparameters (e.g., the learning rate and batch size).

What are the sources of error that causes a model to fail to generalize?

For the sake of visualization, consider 1D least squares regression problem where we know exactly how the ground truth data was generated. 

![[Sources of Test Error-20250823173336275.png]]

Figure 8.3 shows a quasi-sinusoidal function; both training and test data are generated by sampling input values in the range  $[0,1]$, passing them through this function, and adding Gaussian noise with fixed variance.

We fit a simplified [[Shallow Neural Network|shallow neural network]] to this data (figure 8.4). The weights and biases that connect the input layer to the hidden layer are chosen so that the "joints" of the function are evenly spaced across the interval. If there are $D$ hidden units, then these joints will be at $0, \frac{1}{D}, \frac{2}{D}, \dots, \frac{D-1}{D}$.

![[Sources of Test Error-20250823173553205.png]]

This model can represent any piecewise linear function with $D$ equally sized regions in the range $[0,1]$. As well as being easy to understand, this model also has the advantage that it can be fit in closed form without the need for stochastic optimization algorithms (see [[UDL Chapter 8 Problems|problem 8.3]]). Consequently, we can guarantee a global minimum of the loss function during training.